---
description: Project terrain for agents (Codex, Claude, Cursor). Read before non-trivial product work.
alwaysApply: true
---

# The Map (agent terrain)

Canonical **product** status lives in [`library/knowledge/private/product/project-map.md`](../../../library/knowledge/private/product/project-map.md). This rule is the short agent handoff so Codex, Claude Code, or Cursor can resume without inventing progress.

## Current tip

- `main` tip (as of 2026-09-16): `f4b79f7` (PR #61 locally provable go-live code), after `011c53c` (PR #60 PRD-004 docs), `6d5982f` (PR #59 map/ledger sync), `26051b3` (PR #58 PRD-003d), `71c371d` (PR #57 PRD-003c).
- Go-live raid ledger: [`EXECUTION_LEDGER.md`](../../../EXECUTION_LEDGER.md) rows `GGL-001` through `GGL-010` (VERIFIED) and `GGL-B01` through `GGL-B16` (blocked operator asks).
- Batch ledger: [`NEXT_BATCH_LEDGER.md`](../../../NEXT_BATCH_LEDGER.md)
- AC ledger: [`PRODUCTION_EXECUTION_LEDGER.md`](../../../PRODUCTION_EXECUTION_LEDGER.md)
- Marketplace packet: [`highlevel-marketplace-submission.md`](../../../library/knowledge/private/product/highlevel-marketplace-submission.md)
- Latest full reverse review (document-only): [`library/requirements/reports/2026-08-26-full-reverse-review-report.md`](../../../library/requirements/reports/2026-08-26-full-reverse-review-report.md)
- Reverse-review Highs H1-H3 closed on `main` via PR #30. Hygiene M3/M4/M5/M20 closed via PR #31. Dependabot majors ignored via PR #39. Actions major raid (checkout 7 / cache 6 / setup-node 7) is the planned SHA-pinned follow-up.

## Done (do not redo)

- Locally provable PRD-001 work: **267 VERIFIED** / 305 total.
- G1, G4, G8: `ACCEPTED CONSTRAINT` (do not reopen without product owner).
- Raid A nonce CSP Medium closed (2026-08-25).
- Library Schema v2 scaffold gaps closed.
- External Evidence Sprint prep on `main` (evidence packs under `docs/operations/evidence-packs/`).
- G2 App Test **harness** on `main`: env-gated live capture (`OALO_GHL_LIVE_CAPTURE=authorized` only), 9-case matrix, CLI `pnpm ghl:g2-matrix`, sanitization + unit/contract tests. Fail-closed by default.
- PRD-003a campaign persistence on `main` (`70531fb`, PR #54).
- PRD-003b session command context on `main` (`2ee2634`, PR #55).
- PRD-003c human approval on `main` (`71c371d`, PR #57).
- PRD-003d workspace reads on `main` (`26051b3`, PR #58).
- Labeled review dashboard on `main` (`6f64201`, PR #53).
- PRD-004a in-repo code on `main` (`f4b79f7`, PR #61): honest review surfaces (`OALO_REVIEW_SURFACE=authorized`), public-env secret guard, provider default-off proof. `GGL-001`/`GGL-002` VERIFIED in review mode only. `GGL-008`/`GGL-009` DONE (code), BLOCKED (automated gate) as `GGL-B16`.

## Pending / blocked (honest)

| Blocker | Status | What it unlocks |
| --- | --- | --- |
| HighLevel App Test operator run | **Try sandbox + Test Link now** (do not wait for prior Marketplace approval per official docs) | Sanitized fixtures for 28 `DEFERRED: LIVE HIGHLEVEL AUTH` ACs |
| G2 live evidence | Harness ready; **no sanitized fixtures**; do not invent G2 evidence | Flip deferred auth ACs only after `pnpm test:contracts` |
| PRD-003b session command context | **Done** (`2ee2634`, PR #55) | Verified server principal for campaign mutations |
| PRD-003c human approval | **Done** (`71c371d`, PR #57) | Named human approval of exact persisted version |
| PRD-003d workspace reads | **Done** (`26051b3`, PR #58) | Tenant-backed campaign list/detail/overview |
| PRD-004 reviewable go-live | **In work:** [PRD-004](../../../library/requirements/in-work/prd-004-reviewable-go-live/prd-004-reviewable-go-live-index.md); code half done (`f4b79f7`); operator half blocked | `GGL-B01`–`B03` deploy/smoke; `GGL-B04`–`B07` portal/Test Link; `GGL-B09` listing |
| Preview/review smoke (004a) | **Code VERIFIED**; **operator BLOCKED** (`GGL-B01`–`B03`) | Set `OALO_DATABASE_URL` + `OALO_REVIEW_SURFACE=authorized` on `operation-automated-lo-web`; run smoke pack |
| Honest `/overview` on review URL | **Requires** server-only `OALO_REVIEW_SURFACE=authorized` | Without it, default preview still shows labeled synthetic demo metrics (`GGL-001`/`GGL-002` not met) |
| Marketplace portal inspect | **Unsigned-in** (`GGL-B04`–`B06`) | Developer Portal + Test Link (004b) |
| Real-Postgres CI gate (`GGL-B16`) | **Parked** | `pnpm --filter @oalo/db test:postgres` with `OALO_TEST_DATABASE_URL`; not in canonical `pnpm test:db` yet |
| App Test operator access + controlled location | Operator-led | Live OAuth/session matrix capture |
| Env isolation + KMS (Wave 2) | Parked | `001J-AC-026`, `001J-AC-029` |
| G3 / G5 / G6 / G7 live evidence | Parked (later waves) | Meta, lead, billing, counsel |

**Do not** flip any of the 28 deferred auth ACs to `VERIFIED` without sanitized App Test fixtures that pass `pnpm test:contracts`. **Do not** claim production traffic authorized.

## Next steps (librarian)

1. **Human / HighLevel:** Try sandbox + Create App Test Account + Test Link now ([Marketplace packet](../../../library/knowledge/private/product/highlevel-marketplace-submission.md)). Do not invent G2 evidence.
2. **Wave 1:** After operator capture, run [`docs/operations/evidence-packs/g2-highlevel-app-test.md`](../../../docs/operations/evidence-packs/g2-highlevel-app-test.md); ingest sanitized fixtures; update production ledger with evidence pointers. Owner: operator + `gohighlevel-guardian`.
3. **Production tonight (operator):** [`reviewable-preview-smoke.md`](../../../docs/operations/evidence-packs/reviewable-preview-smoke.md) — wire `OALO_DATABASE_URL` + `OALO_REVIEW_SURFACE=authorized` on `operation-automated-lo-web`, run smoke (`GGL-B01`–`B03`), then portal sign-in + Test Link (`GGL-B04`–`B07`). Code for honest review surfaces is on `main` (`f4b79f7`). [`PRD-003`](../../../library/requirements/in-work/prd-003-authenticated-product-activation/prd-003-authenticated-product-activation-index.md) parent exits when operator smoke passes. [`PRD-004c`](../../../library/requirements/in-work/prd-004-reviewable-go-live/prd-004c-reviewable-go-live-marketplace-submission.md) after smoke + Test Link (`GGL-B09`). Do not flip deferred G2 ACs (`GGL-B10`).
4. **Optional parallel:** Wave 2 prep (cloud inventory + KMS custody) if a cloud owner is available; otherwise leave parked.
5. **After any harness/code change:** `security-guardian` then `quality-guardian` (never reverse). Squash-merge only.
6. **Do not** move PRD-001 to `completed/` until project-map core-completion is satisfied.

## Route work

1. Read this map + [`project-map.md`](../../../library/knowledge/private/product/project-map.md).
2. Route via [`.cursor/skills/dungeon-master/SKILL.md`](../../skills/dungeon-master/SKILL.md).
3. For Wave 1 App Test: `gohighlevel-guardian` + `auth-guardian`. For docs/map edits: `library-guardian`.

## Forbidden claims

- Inventing live HighLevel / Meta / Stripe / KMS / counsel evidence
- Reopening G1 / G4 / G8 without product-owner authorization
- Committing tokens, PII, spend, or `OALO_GHL_LIVE_CAPTURE=authorized` as a repo default
