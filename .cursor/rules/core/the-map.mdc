---
description: Project terrain for agents (Codex, Claude, Cursor). Read before non-trivial product work.
alwaysApply: true
---

# The Map (agent terrain)

Canonical **product** status lives in [`library/knowledge/private/product/project-map.md`](../../../library/knowledge/private/product/project-map.md). This rule is the short agent handoff so Codex, Claude Code, or Cursor can resume without inventing progress.

## Current tip

### September 24, 2026 release update

- The authenticated runtime and password/onboarding work merged as PR #67 (`58d77fd`) on September 22. The older C1/unmerged statements below describe the September 16 checkpoint, not the current code.
- The completed dashboard and standalone AVM/homeowner-report implementation merged as PR #69 (`da8dfb9`) on September 24. PR #68 was closed as superseded. The full application, real PostgreSQL/authenticated-browser and release gates passed in run `35951313777`; main verification also passed.
- `operation-automated-lo-web.vercel.app` now serves the authenticated app against the dedicated Supabase project `vonesqpyfsrhasuxfiiz`. Signup, password sign-in, profile persistence, report access and sign-out were exercised on the hosted app. The restricted runtime login uses verified TLS and has no migration-owner membership. See `docs/operations/homeowner-avm-activation.md` and the PRD-007 release reports.
- Live RentCast credentials are still absent. Valuation calls, HighLevel report delivery, paid ads and subscription billing are not activated. The report adapter is implemented; fixture tests are not a live valuation. Only approved workspace UUIDs can receive paid lookup access after activation.
- `fix/authenticated-workspace-pages` finishes the signed-in navigation destinations, saved personal report identity, partner list and message drafts. Its scope and qualification live under `library/requirements/in-work/prd-007-homeowner-reports/reports/`. Preserve these changes when resuming.
- PRD-007 explicitly authorizes the homeowner-report slice of PRD-002e. Other PRD-002 options remain backlog. Do not rebuild authentication or provision another database based on the historical checkpoint below, and do not flip any external acceptance criterion without its evidence.

### Historical September 16 checkpoint

- `main` tip (as of 2026-09-16): `c140f11` (PR #65), preceded by `daa498e` (PR #64) and `86ef97d` (PR #63), after `deeb3e2` (PR #62 production-tonight docs), `f4b79f7` (PR #61 locally provable go-live code), `011c53c` (PR #60 PRD-004 docs), `6d5982f` (PR #59 map/ledger sync), `26051b3` (PR #58 PRD-003d).
- Go-live raid ledger: [`EXECUTION_LEDGER.md`](../../../EXECUTION_LEDGER.md) rows `GGL-001` through `GGL-010` (VERIFIED) and `GGL-B01` through `GGL-B15` (blocked or accepted); `GGL-B16` CLOSED (CI run `35058370796` at `dab2ec6`, PR #65, with PR #66 closed and unmerged).
- Completion review finding C1 (2026-09-19): runtime request authentication is not composed on `main` at `c140f11`. The exported campaign routes and read pages still compose a static local synthetic default for authentication outside synthetic mode, and that default denies every request. Owned by [PRD-005](../../../library/requirements/in-work/prd-005-authenticated-review-runtime/prd-005-authenticated-review-runtime-index.md) (005a, 005b, in progress on branch `claude/completion-review-2026-09-19`, unmerged; pull request #67 opened 2026-09-21); the review URL denies every request without a real session until those sub-PRDs merge.
- Batch ledger: [`NEXT_BATCH_LEDGER.md`](../../../NEXT_BATCH_LEDGER.md)
- AC ledger: [`PRODUCTION_EXECUTION_LEDGER.md`](../../../PRODUCTION_EXECUTION_LEDGER.md)
- Marketplace packet: [`highlevel-marketplace-submission.md`](../../../library/knowledge/private/product/highlevel-marketplace-submission.md)
- Operator runbook (step order, return artifacts, abort conditions): [`production-tonight-operator-runbook.md`](../../../library/knowledge/private/operations/production-tonight-operator-runbook.md)
- Listing copy, shot list, Loom script, claim audit: [`marketplace-listing-copy-pack.md`](../../../library/knowledge/private/product/marketplace-listing-copy-pack.md)
- Latest full reverse review (document-only): [`library/requirements/reports/2026-08-26-full-reverse-review-report.md`](../../../library/requirements/reports/2026-08-26-full-reverse-review-report.md)
- Reverse-review Highs H1-H3 closed on `main` via PR #30. Hygiene M3/M4/M5/M20 closed via PR #31. Dependabot majors ignored via PR #39. Actions major raid (checkout 7 / cache 6 / setup-node 7) is the planned SHA-pinned follow-up.

## Done (do not redo)

- Locally provable PRD-001 work: **267 VERIFIED** / 305 total.
- G1, G4, G8: `ACCEPTED CONSTRAINT` (do not reopen without product owner).
- Raid A nonce CSP Medium closed (2026-08-25).
- Library Schema v2 scaffold gaps closed.
- External Evidence Sprint prep on `main` (evidence packs under `docs/operations/evidence-packs/`).
- G2 App Test **harness** on `main`: env-gated live capture (`OALO_GHL_LIVE_CAPTURE=authorized` only), 9-case matrix, CLI `pnpm ghl:g2-matrix`, sanitization + unit/contract tests. Fail-closed by default.
- PRD-003a campaign persistence on `main` (`70531fb`, PR #54).
- PRD-003b session command context on `main` (`2ee2634`, PR #55).
- PRD-003c human approval on `main` (`71c371d`, PR #57).
- PRD-003d workspace reads on `main` (`26051b3`, PR #58).
- Labeled review dashboard on `main` (`6f64201`, PR #53).
- PRD-004a in-repo code on `main` (`f4b79f7`, PR #61): honest review surfaces (`OALO_REVIEW_SURFACE=authorized`), public-env secret guard, provider default-off proof. `GGL-001`/`GGL-002` VERIFIED in review mode only. `GGL-008` and `GGL-009` VERIFIED in the canonical `pnpm test:db` gate (CI run `35058370796`, PR #65, with PR #66 closed and unmerged, `c140f11`).
- Production-tonight requirements authoring (2026-09-16): **PRD-004d** owns `GGL-B16`; **PRD-004e** holds authored listing content (customer-facing scope statement, FAQ, paste-ready fields, six-frame shot list, Loom script, claim audit); operator runbook + coverage report added. Every parked `GGL-B*` row now has an owning requirement document. Documentation only: no AC status changed.
- PRD-004d real-Postgres command gate is Complete (2026-09-16): route R5 adopted, `GGL-008` and `GGL-009` VERIFIED, CI run `35058370796` at `dab2ec6`, merged as PR #65 (`c140f11`). PR #66 is a closed, unmerged alternate approach to the same gate; it is not reopened.

## Pending / blocked (honest)

| Blocker | Status | What it unlocks |
| --- | --- | --- |
| HighLevel App Test operator run | **Try sandbox + Test Link now** (do not wait for prior Marketplace approval per official docs) | Sanitized fixtures for 28 `DEFERRED: LIVE HIGHLEVEL AUTH` ACs |
| G2 live evidence | Harness ready; **no sanitized fixtures**; do not invent G2 evidence | Flip deferred auth ACs only after `pnpm test:contracts` |
| PRD-003b session command context | **Done** (`2ee2634`, PR #55) | Verified server principal for campaign mutations |
| PRD-003c human approval | **Done** (`71c371d`, PR #57) | Named human approval of exact persisted version |
| PRD-003d workspace reads | **Done** (`26051b3`, PR #58) | Tenant-backed campaign list/detail/overview |
| PRD-004 reviewable go-live | **In work:** [PRD-004](../../../library/requirements/in-work/prd-004-reviewable-go-live/prd-004-reviewable-go-live-index.md); code half done (`f4b79f7`); operator half blocked | `GGL-B01`–`B03` deploy/smoke; `GGL-B04`–`B07` portal/Test Link; `GGL-B09` listing |
| Preview/review smoke (004a) | **Code VERIFIED**; **operator BLOCKED** (`GGL-B01`–`B03`) | Set `OALO_DATABASE_URL` + `OALO_REVIEW_SURFACE=authorized` on `operation-automated-lo-web`; run smoke pack |
| Honest `/overview` on review URL | **Requires** server-only `OALO_REVIEW_SURFACE=authorized` | Without it, default preview still shows labeled synthetic demo metrics (`GGL-001`/`GGL-002` not met) |
| Marketplace portal inspect | **Unsigned-in** (`GGL-B04`–`B06`) | Developer Portal + Test Link (004b) |
| Marketplace listing (`GGL-B09`) | **Content authored** (PRD-004e); capture + submit blocked | Screenshots and Loom need the review URL in review mode; 3 operator values still missing (support email, publisher name, pricing) |
| Real-Postgres CI gate (`GGL-B16`) | **CLOSED** (CI run `35058370796` at `dab2ec6`, PR #65, with PR #66 closed and unmerged, `c140f11`) | Route R5 adopted; `GGL-008` and `GGL-009` VERIFIED in the canonical `pnpm test:db` gate. See [PRD-004d](../../../library/requirements/in-work/prd-004-reviewable-go-live/prd-004d-reviewable-go-live-postgres-command-gate.md) |
| Runtime request authentication (completion review C1) | **Not composed** on `main` at `c140f11`; in progress on branch `claude/completion-review-2026-09-19` (unmerged; pull request #67 opened 2026-09-21) | Owned by [PRD-005](../../../library/requirements/in-work/prd-005-authenticated-review-runtime/prd-005-authenticated-review-runtime-index.md) (005a, 005b); the review URL denies every request without a real session until they merge |
| App Test operator access + controlled location | Operator-led | Live OAuth/session matrix capture |
| Env isolation + KMS (Wave 2) | Parked | `001J-AC-026`, `001J-AC-029` |
| G3 / G5 / G6 / G7 live evidence | Parked (later waves) | Meta, lead, billing, counsel |

**Do not** flip any of the 28 deferred auth ACs to `VERIFIED` without sanitized App Test fixtures that pass `pnpm test:contracts`. **Do not** claim production traffic authorized.

## Next steps (librarian)

1. **Human / HighLevel:** Try sandbox + Create App Test Account + Test Link now ([Marketplace packet](../../../library/knowledge/private/product/highlevel-marketplace-submission.md)). Do not invent G2 evidence.
2. **Wave 1:** After operator capture, run [`docs/operations/evidence-packs/g2-highlevel-app-test.md`](../../../docs/operations/evidence-packs/g2-highlevel-app-test.md); ingest sanitized fixtures; update production ledger with evidence pointers. Owner: operator + `gohighlevel-guardian`.
3. **Production tonight (operator):** follow [`production-tonight-operator-runbook.md`](../../../library/knowledge/private/operations/production-tonight-operator-runbook.md), which sequences [`reviewable-preview-smoke.md`](../../../docs/operations/evidence-packs/reviewable-preview-smoke.md). Wire `OALO_DATABASE_URL` + `OALO_REVIEW_SURFACE=authorized` on `operation-automated-lo-web`, run smoke (`GGL-B01`–`B03`), then portal sign-in + Test Link (`GGL-B04`–`B07`), then capture and submit the authored listing (`GGL-B09`). Code for honest review surfaces is on `main` (`f4b79f7`). [`PRD-003`](../../../library/requirements/in-work/prd-003-authenticated-product-activation/prd-003-authenticated-product-activation-index.md) parent exits when operator smoke passes. [`PRD-004c`](../../../library/requirements/in-work/prd-004-reviewable-go-live/prd-004c-reviewable-go-live-marketplace-submission.md) after smoke + Test Link (`GGL-B09`). Do not flip deferred G2 ACs (`GGL-B10`).
4. **Optional parallel:** Wave 2 prep (cloud inventory + KMS custody) if a cloud owner is available; otherwise leave parked.
5. **After any harness/code change:** `security-guardian` then `quality-guardian` (never reverse). Squash-merge only.
6. **Do not** move PRD-001 to `completed/` until project-map core-completion is satisfied.

## Route work

1. Read this map + [`project-map.md`](../../../library/knowledge/private/product/project-map.md).
2. Route via [`.cursor/skills/dungeon-master/SKILL.md`](../../skills/dungeon-master/SKILL.md).
3. For Wave 1 App Test: `gohighlevel-guardian` + `auth-guardian`. For docs/map edits: `library-guardian`.

## Forbidden claims

- Inventing live HighLevel / Meta / Stripe / KMS / counsel evidence
- Reopening G1 / G4 / G8 without product-owner authorization
- Committing tokens, PII, spend, or `OALO_GHL_LIVE_CAPTURE=authorized` as a repo default
