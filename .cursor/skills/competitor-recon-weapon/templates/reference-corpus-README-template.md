# {target} reference screenshots

> Fill every placeholder. Delete this callout when done. No em dashes anywhere.

Captured {YYYY-MM-DD} by {who} from {which account / product instance}. This is the pixel source of truth for {which parity sweep / PRD / pre-task} and pairs with the written gap tables in `{path to gap-analysis doc}`.

## Layout

- `{Role A}/` - {what these views are}: {list of modules}.
- `{Role B}/` - {what these views are}, one folder per module: {list of modules}.

Files are {raw captures / redacted captures} in visual walk order within each folder.

## Usage rules

- Reference for layout, spacing, states, and component anatomy only. Verify behavioral notes against `{gap-analysis doc}`.
- These are competitor product screenshots held in a PRIVATE repo for internal parity work. Do not publish, embed in public docs, or ship any asset lifted from them.
- PII: these captures contain {list PII types present, e.g. names, emails, phone numbers, balances}. Keep the repo private. Do not paste into external tools. {Redaction status: redacted / pending redaction -- redaction is the required standard per the weapon's Hard Rule 5.}
- Behavior questions: check {competitor public docs location} before guessing; read facts, never copy prose.
